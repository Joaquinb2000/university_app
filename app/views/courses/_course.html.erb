<div class="col s6 m6">
    <div class="card blue-grey darken-1">
    <div class="card-content white-text">
        <span class="card-title"><strong><%= course.short_name %></strong></span>
        <p><%= course.description.truncate(100, separator: " ")%></p>
    </div>
    <div class="card-action">
        <%= link_to "Enroll", "#" %>
        <%= link_to "Info", course_path(course) %>
    </div>
    </div>
</div>
